---
title: The Markdown Fixture
author: A Test Harness
---

# The Markdown Fixture

An opening paragraph with **strong text**, *emphasis*, `a code span with * in it`,
~~a strikethrough~~ and [a link](https://example.invalid/page).

![a remote image that must not be fetched](https://example.invalid/cover.jpg)

## Blocks that have to survive

> A blockquote, which is one paragraph and keeps its own indent.

- first item
  with a continuation line
- second item
- third item

1. ordered one
2. ordered two

| Column | Meaning |
| --- | --- |
| `txt` | plain text |
| `md` | markdown |

```js
const x = 1 * 2   // an asterisk that is not emphasis
```

Setext heading
==============

## Chapter 1

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.


## Chapter 2

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.


## Chapter 3

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.


## Chapter 4

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.


## Chapter 5

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.


## Chapter 6

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

The second matter is verse, which must not be joined at all, because
in verse the line ending is the whole of the argument, and a machine
that tidies it away has destroyed the thing it was asked to display.

The third matter, and the one most often forgotten, is the heading:
plain text has no markup, so a heading can only be recognised by how
it is written, and a machine that guesses too eagerly will put a
line of ordinary prose into the table of contents.

It is a fact universally acknowledged among the makers of reading
machines that a paragraph typed with a hard line ending at the
seventieth column must be joined back into one flowing line before
it is shown to anybody, for otherwise the column of type is set by
the typist and not by the reader.

